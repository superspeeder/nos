# Build Instructions

You need the following dependencies in order to build this:

- GCC cross-compiler for the x86_64-none-elf target **with no red zone**.
- GNU make
- Grub

To test you need

- `qemu-system-x86_64`
